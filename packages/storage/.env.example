MINIO_ENDPOINT=
MINIO_PORT=
MINIO_CONSOLE_PORT=
MINIO_USE_SSL=
MINIO_BUCKET_NAME=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_HOST=
MINIO_REGION=
# Enables browser uploads from your frontend
MINIO_API_CORS_ALLOW_ORIGIN=
# Sets the redirect URL after login to the MinIO Console (port 9001). Useful when accessing the console through a reverse proxy or different domain. If you access the console directly, this may not be needed.
MINIO_BROWSER_REDIRECT_URL=
# Sets the public URL MinIO uses for generating presigned URLs and API responses. Important when MinIO is behind a proxy or accessed via a different domain than the container.
MINIO_SERVER_URL=